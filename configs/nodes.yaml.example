node_pools:
    # 模板池示例：Bright Data 类按地区动态生成账号
    brightdata-example:
        source: template
        enabled: true
        type: http_proxy
        server: brd.superproxy.io
        port: 33335
        username_template: brd-customer-hl_xxxxxxxx-zone-yyy-country-{region}
        password: REPLACE_WITH_YOUR_PASSWORD

    # 直连池
    direct:
        source: direct

    # 静态节点池示例
    static-example:
        source: static
        nodes:
            - name: hk-socks5-1
              type: socks5
              server: example.com
              port: 1080
              region: HK
              username: REPLACE_WITH_USERNAME
              password: REPLACE_WITH_PASSWORD

    # 订阅池示例
    subscription-example:
        source: subscription
        url: https://your-subscription-host/path?token=REPLACE_WITH_TOKEN
        # 留空表示手动刷新；也支持 12h、3d 这类小时/天配置。
        update_interval: 1h
        # 当某地区只有这个订阅池的一个离线节点时，仍允许请求再尝试一次。
        try_offline: false
        reject_regex:
            - 剩余流量|套餐
